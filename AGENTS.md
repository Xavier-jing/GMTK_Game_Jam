# Agent Instructions

## 1. 适用范围与项目背景

本文件适用于仓库根目录及其全部子目录。任何智能体在读取、分析或修改本仓库前都必须遵守本文件。

- 项目类型：短周期 Game Jam 项目。
- 引擎版本：Unity `2022.3.62f3c1`。
- 渲染与玩法基础：2D、Universal Render Pipeline。
- 核心目标：在比赛截止时间内交付可玩的稳定版本，同时保持明确的代码质量底线。

当用户请求与本文件冲突时，智能体必须停止冲突部分，说明原因，并给出由人类完成的操作步骤。

## 2. 人工智能艺术资产禁令

比赛严禁使用人工智能辅助生成的艺术资产。本规则是不可降低的项目合规要求。

智能体不得：

- 生成、编辑、删除、重命名、复制、转换、重采样、压缩或导出任何游戏艺术资产。
- 使用图像、模型、字体、动画、音乐、音效、配音或视频生成工具为项目制作内容。
- 使用 C#、Shader、编辑器脚本或其他自动化方式绕过禁令，生成供游戏使用的视觉或听觉内容。
- 对人类创作的艺术资产进行 AI 扩图、补帧、修复、抠图、风格迁移、变体生成、升采样或重新编曲。
- 自动操作 Unity Editor 保存场景、预制体、材质、动画、导入设置或 Project Settings。

智能体可以只读检查资产路径、Unity YAML、Inspector 字段和导入配置，以理解代码接口并编写人类操作说明。只读检查不得演变为资产修改。

如果任务需要资产、场景或 Inspector 变更，智能体只能：

1. 修改允许的 C# 接口，使其能够接收人类制作的资产。
2. 在 Markdown 文档或最终回复中列出准确的人类挂接步骤。
3. 等待美术、策划或其他人类成员在 Unity Editor 中完成导入和引用。

## 3. 写入白名单

智能体只允许创建、修改、移动或删除以下后缀的文件：

- `.md`：项目规则、设计说明、操作手册和交付记录。
- `.cs`：运行时代码、编辑器代码和 C# 测试代码。

除上述白名单外，其他文件一律只读。特别禁止写入：

- Unity 序列化与元数据：`.meta`、`.unity`、`.prefab`、`.asset`、`.mat`、`.controller`、`.anim`、`.overrideController`、`.mixer`、`.inputactions`。
- 表现层源码或资源：`.shader`、`.shadergraph`、`.hlsl`、`.compute`、`.vfx`、`.uxml`、`.uss`。
- 工程与包配置：`Packages/manifest.json`、`Packages/packages-lock.json`、`ProjectSettings/**`、`.asmdef`、`.asmref`、`.rsp`。
- 图像、模型、字体、动画、音频、视频、压缩包和其他二进制文件。
- Unity、IDE、构建工具或操作系统自动生成的文件。

新增 `.cs` 文件后，智能体不得创建对应 `.meta`，也不得将脚本挂到 GameObject。人类成员必须使用 Unity `2022.3.62f3c1` 导入脚本、检查生成的 `.meta`，完成 Inspector 引用后再提交。

## 4. 任务工作流

### 4.1 开始任务前

1. 阅读本文件，以及与任务相关的 `Documents/` 文档。
2. 运行只读的 Git 状态检查，识别并保护用户已有的修改和未跟踪文件。
3. 只读取与任务直接相关的代码、场景文本和配置；不要无目的扫描或重构整个项目。
4. 确认所有计划写入的文件均在 `.md`/`.cs` 白名单内。

### 4.2 实施规则

- 小型、局部、低风险的代码修复可以直接实施。
- 跨系统重构、新依赖、公共接口调整或大范围迁移必须先给出简短方案和影响范围。
- 修改范围保持最小；不要顺手格式化、重命名或清理无关文件。
- 不得覆盖、还原、暂存或删除用户未授权的既有 Git 变更。
- 不得修改 Packages 或 Project Settings 来解决代码问题；需要此类修改时交接给人类。
- 公共接口或 Inspector 挂接方式变化后，应同步更新对应 Markdown 文档。

### 4.3 资产挂接交接

需要人类在 Unity 中操作时，交接说明至少包括：

- 目标场景、Prefab 或 GameObject 的名称与路径。
- 需要添加的组件及其 Inspector 字段。
- 每个字段应接收的资产类型和推荐取值范围。
- 预期触发时机、失败表现和验证步骤。
- 是否需要生成或提交新的 `.meta`。

不得仅写“请在 Inspector 中配置”，必须给出可执行的具体步骤。

## 5. Game Jam 优先级与范围控制

发生冲突时按以下顺序裁决：

1. 比赛规则与无 AI 艺术资产合规。
2. 截止时间与可提交、可启动、可游玩的构建。
3. 游戏体验正确性和关键流程稳定性。
4. 代码可读性、生命周期安全和可维护性。
5. 功能数量、通用性和未来扩展。

截止时间不可突破时，优先删除或简化功能范围，不得通过提交已知破损代码、跳过关键验证或违反资产禁令换取功能数量。

Game Jam 中不做推测性框架。只有当前需求已有明确调用方时才增加抽象；优先选择最简单、可验证、可在比赛期内维护的实现。

## 6. Unity 与 C# 质量底线

所有 C# 修改必须：

- 兼容 Unity `2022.3.62f3c1` 及其支持的 C#/.NET API，不使用更新 Unity 版本专有接口。
- 保持职责清晰和命名明确；函数嵌套通常不超过三层。
- Inspector 字段优先使用 `private` + `[SerializeField]`，只在确有外部调用方时扩大可见性。
- 明确 `MonoBehaviour` 的 `Awake`、`OnEnable`、`Start`、`OnDisable`、`OnDestroy` 生命周期责任。
- 事件和回调成对订阅、退订；异步任务、Tween 和持久化对象必须说明取消或销毁路径。
- 避免在 `Update` 等高频路径中反射、全局查找、LINQ、字符串拼接和不必要分配。
- 对 Inspector 或外部输入在系统边界进行验证；内部可信路径避免重复防御式代码。
- 不静默吞掉异常或错误状态；日志必须包含足以定位对象或操作的上下文。
- 不为一次性逻辑引入无明确收益的服务、注册表、基类或配置层。

## 7. 验证与完成标准

代码任务至少完成以下验证：

1. 执行不会写入禁用文件的 C# 编译或静态检查；无法安全运行时明确说明原因。
2. 给出与修改直接相关的 Unity Play Mode 冒烟步骤，供人类在指定编辑器版本中执行。
3. 再次检查 Git 状态和差异，确认没有新增或修改白名单之外的文件。
4. 不得在未验证时宣称任务已完全完成。

关键流程的人工冒烟范围包括：

- 从 `Boot` 进入 `MainMenu`。
- 从主菜单进入 `GamePlay` 和 `SandBox`。
- HUD、暂停菜单、设置菜单和返回主菜单。
- 加载遮罩、进度显示、BGM、SFX 和音量持久化。

文档任务至少执行链接检查、术语/路径核对和 `git diff --check`。

## 8. Git 与安全边界

- 删除、移动或重命名文件前先确认其跟踪状态和任务授权。
- 不得使用破坏性 Git 命令处理用户工作。
- 只暂存与当前任务直接相关的 `.md`/`.cs` 文件。
- 完成时列出修改文件、验证结果、尚需人类完成的 Unity 操作。
- 发现任务外变更时保留原状；只有它直接阻塞任务时才向用户报告。

